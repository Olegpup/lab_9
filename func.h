#pragma once
#include <iostream>
#include <string>
using namespace std;

int words_count(std::string str);
std::string* string_cutter(std::string str, int words_num);
void searcher(std::string* words, int lenght, int number);